describe('audio', function () {
    var $audio;

    beforeEach(function () {
        $audio = document.createElement('audio');
    });

    it('audio srcを読み込む', function () {
        expect(true);

    });
});
